apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-app-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: '104.197.151.22.nip.io'  # Aquí se usa el formato correcto
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: go-app-service
                port:
                  number: 80
